<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Warehouse Report - Fabric ERP</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root { --bg-beige: #f4f1ea; --fabric-green: #198754; --fabric-dark: #212529; }
        body { background-color: var(--bg-beige) !important; font-family: 'Segoe UI', sans-serif; color: #333; }
        .card { border: none; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
        .report-header { border-bottom: 2px solid var(--fabric-green); padding-bottom: 10px; margin-bottom: 30px; }
        .table-custom-head { background-color: var(--fabric-green); color: white; }
        
        @media print {
            body { -webkit-print-color-adjust: exact !important; print-color-adjust: exact !important; background-color: white !important; }
            .table-custom-head { background-color: var(--fabric-green) !important; color: white !important; }
            .no-print, .btn { display: none !important; }
            .container { max-width: 100% !important; width: 100% !important; margin: 0 !important; padding: 0 !important; }
            .card { box-shadow: none !important; border: 1px solid #ddd !important; }
        }
    </style>
</head>
<body>
    <div class="container py-5">
        <div class="d-flex justify-content-between align-items-center report-header">
            <div>
                <h1 class="fw-bold text-dark">Inventory Report</h1>
                <p class="text-muted mb-0">Fabric ERP System</p>
            </div>
            <div class="text-end">
                <span class="badge bg-dark fs-6 px-3 py-2">Date: <%= date %></span>
            </div>
        </div>

        <div class="card mb-4">
            <div class="card-body p-4">
                <h5 class="fw-bold" style="color: var(--fabric-green);">Executive Summary</h5>
                <hr>
                <div class="row text-center">
                    <div class="col-6">
                        <p class="mb-1 text-muted small uppercase">Total Items</p>
                        <h3 class="fw-bold"><%= products.length %></h3>
                    </div>
                    <div class="col-6 border-start">
                        <p class="mb-1 text-muted small uppercase">Total Asset Value</p>
                        <h3 class="fw-bold text-success"><%= totalValue %> €</h3>
                    </div>
                </div>
            </div>
        </div>

        <div class="card overflow-hidden">
            <table class="table table-striped mb-0 align-middle">
                <thead class="table-custom-head text-center">
                    <tr>
                        <th class="py-3 ps-4 text-start">Product Name</th>
                        <th class="py-3">Category</th>
                        <th class="py-3">Price (€)</th>
                        <th class="py-3">Stock Level</th>
                    </tr>
                </thead>
                <tbody class="text-center">
                    <% products.forEach(function(product) { %>
                        <tr>
                            <td class="ps-4 fw-bold text-start"><%= product.name %></td>
                            <td><span class="badge bg-light text-dark border"><%= product.category %></span></td>
                            <td><%= product.price %></td>
                            <td>
                                <% if (product.stock < 5) { %>
                                    <span class="badge bg-danger">Low Stock (<%= product.stock %>)</span>
                                <% } else { %>
                                    <span class="badge bg-success text-white"><%= product.stock %> Units</span>
                                <% } %>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>

        <div class="d-flex justify-content-center gap-3 mt-5 no-print">
            <a href="<%= typeof clientUrl !== 'undefined' ? clientUrl : '/' %>" class="btn btn-outline-dark px-4 fw-bold">← Dashboard</a>
            <% if (typeof role !== 'undefined' && (role === 'admin' || role === 'store_manager')) { %>
                <button onclick="window.print()" class="btn btn-success px-4 fw-bold shadow-sm">Save as PDF</button>
            <% } %>
        </div>
        
        <footer class="text-center text-muted mt-5 pt-3 border-top small">
            <p>Generated by Fabric ERP System &copy; <%= new Date().getFullYear() %></p>
        </footer>
    </div>
</body>
</html>